/**
 * Clases compartidas de la UI — única fuente para inputs y botones.
 * Cambiar el estilo de marca aquí lo cambia en toda la app.
 */

/** Input de formulario (ancho completo). */
export const INPUT =
  "w-full rounded-lg border border-neutral-300 px-3 py-2 text-sm outline-none focus:border-neutral-900";

/** Input de barra de filtros (ancho natural). */
export const INPUT_INLINE =
  "rounded-lg border border-neutral-300 px-3 py-2 text-sm outline-none focus:border-neutral-900";

/** Input compacto (formularios densos, ej. catálogo). */
export const INPUT_SM =
  "rounded-lg border border-neutral-300 px-2 py-1.5 text-sm outline-none focus:border-neutral-900";

/** Input dentro de celdas de tabla. */
export const INPUT_TABLA =
  "w-full rounded border border-neutral-300 px-2 py-1 text-sm outline-none focus:border-neutral-900";

/** Botón primario (acción principal de la vista). */
export const BTN_PRIMARIO =
  "rounded-lg bg-neutral-900 px-4 py-2 text-sm font-medium text-white hover:bg-neutral-800";

/** Botón secundario. */
export const BTN_SECUNDARIO =
  "rounded-lg border border-neutral-300 px-3 py-1.5 text-sm text-neutral-700 hover:bg-neutral-100";
